# .npmignore — overrides .gitignore for npm pack/publish
# The root .gitignore has `*.exe` which would exclude vendor binaries.
# This file restores them so the installer can bundle them.

# Allow everything from `vendor/` (binary files needed at install time)
!vendor/*.exe
